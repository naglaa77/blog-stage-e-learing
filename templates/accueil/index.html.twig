{% extends 'base.html.twig' %}

{% block title %}Hello AccueilController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { padding: 2px 6px; }
</style>

{# First section #}
   <section id="slide1">
        <div class="container">
          
            <article class="row g-2">
              
                <div class="col-md-4">
                        <h2>Pour construire & améliorer</h2>
                        <p>vos compétences techniques</p>
                      <div>
                         <button type="button" class="btn btn-lg cta-perso-couleur">
                          <a href="/Blogues.html">Commencez maintenant</a></b></button>                
                      </div>
                </div>
                <div class="col-md-4">
                    <img src="../build/img/slide.jpg" alt="" class="col-6">
                </div>
            </article>  
        </div>
    </section>

{# Second Section #}

    <section id="galerie">
        <div class="container">
            <article class="row g-2">
                <img src="../build/img/slid3.jpg" alt="" class="col-6">
                <div class="col">
                        <h3>Un pessimiste voit la difficulté dans chaque opportunité, un optimiste  voit l’opportunité dans chaque  difficulté.</h3>
                </div>
            </article>              
        </div>
    </section>


{# Third Section #}


    <section id="slide2">
        
        <div class="container">
            <article class="row g-2">
                
                <div class="col">
                    <h3>Un pessimiste voit la difficulté dans chaque opportunité, un optimiste  voit l’opportunité dans chaque  difficulté.</h3>
                </div>
                <img src="../build/img/slide2.jpg" alt="" class="col-6">
            </article>              
        </div>
    </section>


{# Section Blogs #}


<section id="blogs" class="fu">
    <h3 class="titre-perso container">
        Derniers Blogs
    </h3>
<div id="blog" class="container">
{% for article in latestArticles %}
      <div id="cardb">
      <a href={{ path('blogues_show', {'slug' : article.slug}) }}>
        <img src="{{ article.image }}" alt="Cours" style="width:100%">
        <h1>{{ article.title }}</h1>
        <h6>{{ article.createdAt | date('Y-m-d') }}</h6>
        <div id="blue"></div>
        </a>
      </div>
{% endfor %}

</div>
</section>

{# Section Courses #}
    <section id="cours1">
      <h3 class="titre-perso container">
      Cours de Formation
      </h3>


		<div class="main-container"><ul class="grid-wrapper">
{% for cours in latestCourss %}
			
				<a href={{ path('cours_show', {'slugC' : cours.slugC}) }}>
                     <li id="card">
                        <div >
                            <img src="{{ cours.imageC }}" alt="Cours" style="width:80%">
                            <h4>{{ cours.titleC }}</h4>
                            <p>{{ cours.descriptionC }}</p>
                        </div>
                    </li>
                  </a>
            
{% endfor %}            
		</ul></div>

	</section>

{% endblock %}
