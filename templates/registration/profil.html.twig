{% extends 'base.html.twig' %}

{% block title %}profil {{profil.email}}{% endblock %}

{% block body %}
<br><br>
    <div class="container">
        <div class="row">
            <div class="bg-light mt-10 p-4">
                <h1>Modification Mon Prenom</h1>
                {{ form_start(formView) }}
                    {{ form_row(formView.prenom) }}
                    {# {{ form_row(registrationForm.plainPassword, {
                        label: 'Mot de passe'
                    }) }} #}
                    {{ form_row(formView.plainPassword) }}
                    
                   
                    <button type="submit" class="btn btn-lg cta-perso-couleur">Mettre A Jour</button>
                {{ form_end(formView) }}
            </div>
        </div>
    </div>
<br><br>

{% endblock %}
